# Use an official Node.js runtime as a parent image
FROM node:16

# Set the working directory in the container
WORKDIR /usr/src/app

# Install MQTT client and other dependencies
RUN npm install -g mqtt

# Install any additional tools you might need
